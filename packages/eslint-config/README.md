# EsLint Config

## Usage

### Install

``` bash
yarn add @hongbusi/eslint-config --dev
```

### Extend this config

In `package.json`:

``` json
{
  "eslintConfig": {
    "extends": "@hongbusi"
  }
}
```
